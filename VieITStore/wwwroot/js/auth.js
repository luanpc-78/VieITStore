document.querySelectorAll('[data-password-toggle]').forEach(button=>{button.addEventListener('click',()=>{const input=button.parentElement?.querySelector('input');if(!input)return;const show=input.type==='password';input.type=show?'text':'password';button.setAttribute('aria-label',show?'Ẩn mật khẩu':'Hiện mật khẩu');const icon=button.querySelector('i');if(icon)icon.className=show?'fas fa-eye-slash':'fas fa-eye';});});
document.querySelectorAll('[data-auth-form]').forEach(form=>{form.addEventListener('submit',()=>{if(window.jQuery&&typeof window.jQuery(form).valid==='function'&&!window.jQuery(form).valid())return;const button=form.querySelector('[type="submit"]');if(!button)return;button.disabled=true;button.querySelector('[data-submit-label]')?.classList.add('opacity-75');button.querySelector('[data-submit-spinner]')?.classList.remove('d-none');});});
